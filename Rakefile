# -*- coding: utf-8 -*-
$:.unshift("/Library/RubyMotion/lib")
require 'motion/project'
require 'motion-cocoapods'
require 'bubble-wrap'

Motion::Project::App.setup do |app|
  app.name = 'Pathways'
  app.identifier = 'nl.plict.Pathways'

  app.pods do
    pod 'MBProgressHUD'
  end
end
